# Optional odds aggregation source.
THE_ODDS_API_KEY=

# Required for remote login on Vercel.
# Generate AUTH_SECRET with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=

# Generate with: pnpm hash:access-code "your-private-access-code"
SITE_ACCESS_CODE_SHA256=
